refactor <netlify-form> component to to take storyblok items instead of schema.

component was causing issues with static generation as it was trying to minipulate the DOM after render through programmatic field generation. Form now takes content blocks like other content components.